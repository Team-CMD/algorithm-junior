#include <stdio.h>
int main()
{
    int t,n,m,a,b;
    
    scanf_s("%d", &t);
    
    while (t--)
    {
        scanf_s("%d %d", &n, &m);
        
        for (int i = 0; i < m; i++)
        {
            scanf_s("%d %d", &a, &b);
        }
        printf("%d\n",n - 1);
    }
    return 0;
}
